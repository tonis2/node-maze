const solveMaze = (data ) => {
   return new Promise ((resolve, reject) => {
      const playfield = data.playfield;
      const start = data.start;
      const end = data.end;
      let maze = [];
      data.playfield.forEach( state => {
        
      });
      resolve("yMax");
  });
}

module.exports = {
  solveMaze
}
